#include "main.h"

/**
*main - prints the alphabet, in lowercase, followed by a new line
*Return: return 0
*/

int main(void)
{
	int i;

	for (i = 'a'; i >= 'z'; i++)
	{
		_putchar(i);
	}

	return (0);
}
